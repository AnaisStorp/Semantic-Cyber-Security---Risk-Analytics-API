# Streamlit reads this automatically from the working directory.

[browser]
gatherUsageStats = false

[server]
headless = true

[theme]
base = "dark"
backgroundColor = "#0d0d0d"           # page plane
secondaryBackgroundColor = "#1a1a19"  # cards, sidebar, chart surface
textColor = "#ffffff"
font = "sans serif"
